# Maze
```
+---+---+---+---+
|               |
+---+   +   +   +
|               |
+   +---+   +   +
|               |
+   +   +---+   +
|               |
+---+---+---+---+
```


Программа для генерации лабиринтов и поиска кратчайшего пути с использованием текстового интерфейса (ncurses).

В проекте реализован алгоритм генерации лабиринтов на основе метода Эллера и поиск кратчайшего пути с помощью BFS (поиска в ширину). Интерфейс построен с использованием библиотеки ncurses.

## Зависимости

* **ncurses** — для текстового интерфейса
* **Check** — для модульного тестирования
* **gcovr** — для генерации отчета о покрытии кода

## Сборка

```bash
make
```

## Запуск

```bash
./bin/maze
```

## Установка

```bash
make install
```

По умолчанию программа устанавливается в `/usr/local/bin`. Для изменения пути установки используйте:

```bash
make install PREFIX=/custom/path
```

## Сборка документации

```bash
make dvi
```


Документация будет сгенерирована в директории `docs/`.

## Запуск тестов

```bash
make tests
```

## Покрытие кода

```bash
make gcov
```

Отчет будет сохранен в файл `coverage.html`.

## Очистка проекта

### Очистка объектных файлов и бинарников

```bash
make clean
```

## Поток работы программы

1. **Инициализация** — запуск ncurses и цветовых схем
2. **Выбор источника лабиринта**:
   - `[1]` — загрузить из файла
   - `[2]` — сгенерировать случайный
   - Любая другая клавиша — выход
3. **При загрузке из файла** — ввод имени файла
4. **При генерации**:
   - Ввод размера лабиринта (rows cols)
   - Ввод имени файла для сохранения
5. **Ввод координат** — начальной и конечной точек (row col)
6. **Отображение** — лабиринт с найденным путем (отмечен символом `*`)
7. **Завершение** — экран прощания, очистка памяти

## Формат файла лабиринта

```
rows cols
right_walls[0][0] right_walls[0][1] ... right_walls[0][cols-1]
...
right_walls[rows-1][0] ... right_walls[rows-1][cols-1]
bottom_walls[0][0] bottom_walls[0][1] ... bottom_walls[0][cols-1]
...
bottom_walls[rows-1][0] ... bottom_walls[rows-1][cols-1]
```

Где `1` означает наличие стены, `0` — отсутствие.

**Пример файла 3×3:**
```
3 3
0 0 1
1 0 0
0 0 1
1 0 1
0 0 0
1 1 1
```

## Структура проекта

```
├── Makefile                      # Сборка проекта
├── README.md                     # Документация
├── bin/                          # Исполняемые файлы (создается при сборке)
├── include/                      # Заголовочные файлы
│   ├── ERR_maze.h                # Определение типов ошибок
│   ├── frontend.h                # Заголовочный файл интерфейса
│   ├── maze.h                    # Заголовочный файл лабиринта
│   ├── maze_image.h              # Заголовочный файл изображения
│   └── queue.h                   # Заголовочный файл очереди
├── frontend.c                    # Пользовательский интерфейс
├── main.c                        # Точка входа в программу
├── maze.c                        # Логика работы с лабиринтом
├── maze_image.c                  # Создание изображения лабиринта
├── queue.c                       # Реализация очереди для BFS
└── tests/                        # Модульные тесты
    └── test_find_path.c          # Тесты
```
